branches:
    only:
    - master

test: off

before build:
	- dotnet restore

build:
	- dotnet build

after_test:
  - dotnet pack .\src\* --configuration=Release --output=".\Nuget"

artifacts:
  - path: .\Nuget\*.nupkg